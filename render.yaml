services:
  - type: web
    name: streamlit-agent-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run agents/st.py --server.port $PORT --server.address 0.0.0.0
